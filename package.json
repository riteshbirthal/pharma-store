{
  "name": "pharma-store",
  "version": "1.0.0",
  "private": true,
  "description": "Simple React app for Shree Shyam Medicos shop",
  "scripts": {
    "start": "parcel index.html",
    "dev": "parcel index.html --open",
    "build": "parcel build index.html --dist-dir dist",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "homepage": "https://riteshbirthal.github.io/pharma-store",
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-router-dom": "^6.18.0"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "parcel": "2.16.3"
  }
}
